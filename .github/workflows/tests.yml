name: Run Tests

on:
  push:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Running unit tests..."
          sleep 5
          echo "✅ Tests passed!"
